import { TemplateOverrideDirective } from './template-override.directive';

describe('TemplateOverrideDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateOverrideDirective();
    expect(directive).toBeTruthy();
  });
});
